# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment (public-safe VITE_ values, but untracked as of WS-A)
.env
.env.local
deno.lock
tsconfig.app.tsbuildinfo
tsconfig.node.tsbuildinfo

# Local CI parity runner artifacts
.ci-logs/
services/voice-gateway/vad.out

# Wake-word training scratch: multi-GB downloads, two nested git clones, and the
# ACAV100M feature set — which is CC BY-NC-SA 4.0 and must never enter this repo.
scripts/train-wakeword/work/
scripts/train-wakeword/output/
